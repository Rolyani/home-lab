apiVersion: extensions.kubeblocks.io/v1alpha1
kind: Addon
metadata:
  name: mysql
spec:
  type: Helm
  helm:
    chartLocationURL: kubeblocks-addons/mysql
  defaultInstallValues:
    - enabled: true
      persistentVolumeEnabled: true
      replicas: 1
      storageClass: longhorn
